"""
Information that needs to be present:
- Identifying ID (PK), either iTunes's PID or the raw track ID
- Last (synchronized) playcount recorded
- Filename on disk (if cut, then the location of the copy on disk)
- Filename on phone (which is only used to figure out what track is which from the bpstat)
- Song name (for use with bpstat)
- Album name
- Artist name
"""
